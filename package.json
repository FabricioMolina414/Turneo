{
  "name": "turneo",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "db:generate": "pnpm -C packages/db exec prisma generate --schema prisma/schema.prisma",
    "db:migrate": "pnpm -C packages/db exec prisma migrate dev --schema prisma/schema.prisma",
    "db:studio": "pnpm -C packages/db exec prisma studio --schema prisma/schema.prisma",
    "up": "docker compose up -d",
    "down": "docker compose down"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prisma": "^6.16.1",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  }
}